<h1>Jobs</h1>

<table>
  <thead>
    <tr>
      <th>Company</th>
      <th>Position</th>
      <th>Salary</th>
      <th>Paid or not?</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.company %></td>
        <td><%= job.position %></td>
        <td><%= job.salary %></td>
        <% if current_user.admin? %>
          <td><%= link_to 'Show', job %></td>
          <td><%= link_to 'Edit', edit_job_path(job) %></td>
          <td><%= link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% else %>
          <% if job.paid_at %>
            <td><%= button_to 'Paid', root_path, class: "btn btn-success disabled" %></td>
          <% else %>
            <td><%= button_to 'Pay', root_path, class: "btn btn-primary" %></td>
          <% end %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if current_user.admin? %>
  <%= link_to 'New Job', new_job_path %>
<% end %>
